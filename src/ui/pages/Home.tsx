import React from 'react';

const Home: React.FC = () => (
  <section>
    <h1>Welcome to Proviamo</h1>
    <p>This is a simple demo application built with Node, Express, TypeScript, PostgreSQL, Docker and React.</p>
  </section>
);

export default Home;